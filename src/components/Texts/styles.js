import styled from "styled-components";

const StyledText = styled.p`
    color: var(--grey);
    font-size: 1.6rem;
    line-height: 35px;
    text-align: center;
    margin-left: auto;
    margin-right: auto;
    width: 80%;
`

export {StyledText}